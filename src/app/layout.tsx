import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "G-FILES - GDPR-Compliant Enterprise File Sharing Platform",
  description: "The most secure, GDPR-compliant file sharing platform designed for enterprises, legal firms, and organizations that demand the highest standards of data protection and privacy compliance.",
  keywords: "GDPR compliance, secure file sharing, enterprise security, data protection, privacy compliance, file management, secure document sharing",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
